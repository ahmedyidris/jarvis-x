---
title: 2026-08 changelog
---

# August 2026

Monthly summary — full commit-by-commit detail in `git log` and `SESSION_FINAL_REPORT.md` (repo root).

## Security & correctness
- Kill switch now checked by every action type in the JS agent-autonomy path (was previously unchecked for `agent.js`) and by `app.py`'s `/api/ask` (see [[api-ask-kill-switch-gating]])
- Shell-injection gap closed (raw `child_process.exec` → existing allowlist); a deeper symlink-jail bug found and fixed alongside it
- `scheduler.js`'s validation gate fixed — was silently rejecting 100% of proposals
- `paper-trading.js`'s P&L calculation fixed — was structurally always `0` regardless of market movement
- `test-helper.js`'s async-test bug fixed — a real, previously-latent leaked-kill-switch-file risk
- `app.py`'s `/api/status` fixed — was computing a real conversation count and discarding it

## Phase B
- [[commodities-macro]] vertical added (Week 3)
- [[geopolitical-risk]] vertical added (Week 4) — surfaced the numeric-fidelity gap documented on that page

## Architecture decisions
- [[model-gateway-not-wired]] — re-verified and confirmed
- [[elevenlabs-removed]]
- [[docker-single-container]]

## Deployment
- Docker (`Dockerfile`, `docker-compose.yml`) — single container, not the originally-specified two-service split; see [[docker-single-container]]. **Now built and running end-to-end, verified live** (see below) — the disk-risk caveat from earlier in the month no longer applies.
- Command-center Dashboard added to `web/` + `app.py` (4 tabs: Observatory, Market Data, Video Pipeline, Configuration). Extends the existing FastAPI backend rather than standing up a separate Express server; no fake config controls; no light-mode toggle (the app's dark HUD theme is a documented single-theme design, not a gap). Full reasoning and verification detail in commit `2bbdc1b`.
- Cron installed (wasn't present at all) — one job: nightly `scripts/status.sh` run. "Log rotation" was explicitly declined: it would mean rotating `logs/*.jsonl`, which `CONSTITUTION.md` forbids ("logs are never edited, only appended").
- GitHub Actions CI added, scoped to the 6 of 11 JS tests that don't need local Ollama/Piper/Kokoro infrastructure a hosted runner lacks, plus `model-gateway` and the `web` build.

### Docker: from disk-blocked to running healthy
5 local `docker build` attempts on the dev machine's Crostini container all hit the same real, reproducible wall: the pip-install layer's commit step needs more disk headroom than this 35GB-allocation container has, confirmed across two different starting-disk levels (11GB and 13GB free) with identical failure points. Along the way, 3 real dependency conflicts were found and fixed (not guessed): `omnivoice` (+6 packages that existed only to support it) pinned a git commit whose current metadata requires `transformers>=5.3.0`, conflicting with this repo's load-bearing `transformers==4.42.4`; `kokoro-onnx`'s declared `numpy>=2.0.2` floor is stricter than what it actually needs at runtime (verified against the real, working `numpy==1.26.4` environment) — moved to a `--no-deps` follow-up install. `--no-cache-dir` added everywhere after confirming pip's build-time wheel cache was roughly doubling peak disk usage during that one step.

Given the local disk ceiling, the build moved to GitHub Actions (`.github/workflows/docker-build.yml`, manual dispatch, pushes to `ghcr.io/ahmedyidris/jarvis-x`) — succeeded there on the first run. Pulling the image back down locally then hit its own real issue: a flaky connection producing repeated `unexpected EOF` on specific layers, fixed by tuning `/etc/docker/daemon.json` (`max-concurrent-downloads: 2`, `max-download-attempts: 10`) rather than blindly retrying. `docker-compose up` then surfaced a genuine port conflict — this same machine's live bare-metal deployment already owns `127.0.0.1:8000` and `127.0.0.1:11434` — fixed by remapping the container to host port 8001 and dropping the (already-optional) 11434 host exposure.

End state, verified live, not assumed: container reaches `Up (healthy)`, `curl http://localhost:8001/api/status` returns real data (all TTS voices loaded — Piper, Kokoro, MMS, EGTTS), startup logs show a clean supervisord handoff with all 4 Ollama models recognized via the bind-mount (no re-download). The bare-metal `hermes-api`/`ollama` deployment's uptime was confirmed unbroken throughout every step of this — builds, pulls, a hung concurrent build that had to be killed, port remapping.

## Documentation
- `docs/architecture.md`, `docs/DEVELOPMENT.md` created; `README.md` rewritten (was a Week-1 snapshot describing a deleted file as the live router)
- This vault created

## Dependencies
- `automation/n8n` bumped in-range; `sentinel/requirements.lock.txt` added (pinned, pip-audit clean)
