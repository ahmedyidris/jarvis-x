
.agents/
# and worktrees are full nested checkouts that must never be committed.
awesome/
awesome-llm-apps/
awesome-mcp-servers/
*.bak
*.bak2
# Claude Code local/derived state — regenerable from skills-lock.json,
.claude/settings.local.json
.claude/skills/
.claude/worktrees/
# regenerable via CONTEXT.md's "Agency Agents personas" section
.claude/agents/
# local cache dir created by the global token-optimizer-mcp plugin, not jarvis-x's own code
.token-optimizer/
# regenerable graphify CLI output (graph.json/graph.html/GRAPH_REPORT.md) - a personal
# navigation aid over this repo, not part of it; can appear at the repo root and under
# docs/obsidian-vault/
graphify-out/
DeepSeek-V3/
ds4/
electron/dist/
electron/node_modules/
.env
.env*
!.env.template
free-for-dev/
jarvis-x_*.deb
langflow/
logs/
logs/plans.jsonl
logs/queue.jsonl
logs/scheduled.jsonl
logs/scratch.txt
memory/observed.jsonl
# The bitemporal fact store (code/memory-bitemporal.js). Agent-written runtime
# data like observed.jsonl above, and per-machine: the Chromebook's facts are
# not the container's. Ignoring it also keeps code/weekly-sweep.js from
# reporting it as a stale reference, since that detector skips ignored paths.
memory/facts.jsonl
# The human inbox for parked memory repairs (code/memory-repair.js). Same
# reasoning as facts.jsonl: agent-written runtime data, per-machine.
memory/repair-inbox.jsonl
models/
node_modules/
ollama/
open-design/
OpenHands/
public-apis/
*.pyc
**/__pycache__/
__pycache__/
Qwen3.6-27B-AEON-Ultimate-Uncensored-DFlash/
Scrapling/
scripts/verify/output/
sentinel/data/vector_store.db
sentinel/eval/eval_report.json
# sentinel/ (portfolio subproject — separate from the jarvis-x agent)
sentinel/.venv/
quantum/.venv/
voices/
*.sw[a-p]
.worktrees/
